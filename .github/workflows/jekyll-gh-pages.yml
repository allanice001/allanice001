name: Pages README as homepage

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - name: Prepare docs from README
        run: |
          mkdir -p docs
          # If README.md doesn't already have Jekyll front matter, add a tiny one
          if ! head -n 1 README.md | grep -q '^---$'; then
            {
              echo '---'
              echo 'title: Home'
              echo 'layout: default'
              echo '---'
              echo
              cat README.md
            } > docs/index.md
          else
            cp README.md docs/index.md
          fi

          # Minimal Jekyll config (pick any supported theme)
          cat > docs/_config.yml <<'CFG'
          theme: jekyll-theme-cayman
          title: Allanice001
          description: Profile README rendered on GitHub Pages
          CFG

      - name: Setup Pages
        uses: actions/configure-pages@983d7736d9b0ae728b81ab479565c72886d7745b # v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa # v3
        with:
          path: docs

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
